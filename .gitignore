# local build/test output
/dist/
/portable/
/bin/
*.log

# benchmark results (step 0 harness)
/results/

# tor runtime data (kept next to the packaged tor.exe, never in the repo)
data/

# locally compiled launcher + helper (CI builds them into the artifact)
/scripts/start-tor.exe
/scripts/tun-helper.exe
